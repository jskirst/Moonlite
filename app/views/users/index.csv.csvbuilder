csv << ["Name", "Email", "Motto", "# Enrolled Paths", "# Completed Paths", "Created At Date", "Last Action Date"]
@users.each do |u|
  csv << [u.name, u.email, u.catch_phrase.to_s, u.usage("enrolled_paths"), u.usage("completed_paths"), u.created_at, u.usage("last_action")]
end