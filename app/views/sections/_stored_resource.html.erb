<% if stored_resource && stored_resource.obj %>
  <% if stored_resource.obj.content_type.include?("image") %>
    <img src="<%= stored_resource.obj.url.gsub("::",":") %>" style="max-width: 750px; margin-left: 150px; margin-bottom: 30px;">
  <% end %>
<% end %>