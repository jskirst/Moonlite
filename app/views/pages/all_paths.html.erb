<h1>All Paths</h1>
	<% unless @paths.empty? %>
	<table class="paths" summary="Users paths">
		<%= render @paths %>
	</table>
	<%= will_paginate @paths %>
	<% end %>
<%= link_to "Create new Path", new_path_path %>