%div.single_pane  = link_to "Create New Challenge", new_path_path, class: "button button-info", style: "float:right; margin-top: 5px"  %h1 My Creations  %hr  %div{ style: "margin-bottom: 40px;" }    - if @paths.empty?      %p You have not created any Challenges yet. You can get started by #{link_to "creating one here", new_path_path}    - else    %ul.media-grid      - @paths.each do |path|        %li          = link_to edit_path_path(path) do            %div.path-caption              %span.caption-title= truncate(path.name, :length => 35)          = image_tag path.path_pic, class: "thumbnail", size: "210x150"