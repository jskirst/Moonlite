<h1>New Picture</h1>
<p>You can upload a picture for any task and have it displayed just below the question. You can use this for "identify this" type questions or just for some added visuals.</p>
<%= form_tag({:controller => "tasks", :action => "add_stored_resource"}, :html => { :multipart => true, :method => :put })  do |f| %>
  <%= render "shared/error_messages", :object => f.object %>
  <div class="field clearfix">
    <%= f.label :description %>
    <div class="input">
      <%= f.text_area :description, :class => "xxlarge", :rows => "2", :placeholder => "optional..." %>
    </div>
  </div>
  <div class="field">
    <%= f.label "Upload File" %>
    <div class="input">
      <%= f.file_field :obj %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit "Save", :class => "btn primary" %>
  </div>
<% end %>