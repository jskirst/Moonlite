%div.explorenewusercontent
  :javascript
    $(function(){
      $(".persona_select").on("ajax:success", function(xhr, data){
        $(".persona_select.active").removeClass("active");
        $(this).addClass("active");
        $(".exploretiledetailsarea").replaceWith(data);
      });
      $(".persona_select:first").addClass("active");
      close_button();
    });
  %div.exploreheader
    %div.modal_close
      %img{:src => "https://s3.amazonaws.com/moonlite-nsdub/static/close_icon.png"}
    %h1{ style: "font-size: 23px; margin-bottom: 3px;" }
      %strong Explore: Adopt a Persona...
    %div
      %img{ src: "https://s3.amazonaws.com/moonlite-nsdub/static/Lightbulb+icon", style: "width:15px; position:relative; top:1px;" }
      %p{ style: "font-size: 14px; line-height: 28px; display:inline; "} Your Persona should represent you and your interests. You can always enroll in multiple Personas. 
  %hr{ style: "margin:0px;" }
  %div.exploretiles
    - @personas.each do |persona|
      %div.singleexploretile
        = link_to preview_persona_path(persona), class: "persona_select", remote: true do
          %div.exploretileimage
            = image_tag persona.picture, size: "85x85"
  = render "personas/preview", { persona: @personas.first }
