<div class="user_info">
	<a href="<%= user_path(current_user) %>">
		<span class="user_name">
			<%= current_user.name %>
		</span>
		<span class="modules">
			<% pluralize(current_user.paths.count, "module") %>
		</span>
	</a>
</div>